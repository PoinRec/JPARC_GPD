# JPARC_GPD
GEANT4 scripts for designing facilities layout of a GPD measurement experiment at JPARC.

## Usage
Just run these commands in your terminal:
```bash
mkdir build
cd build
cmake ..
make
./jparc
```